extends layout
 
mixin list-ord
 ol 
  each val in courses
   li
      a(href="/courses/"+val.id)= val.name
 
block content
  p= JSON.stringify(courses)
  h1 Courses 
  +list-ord
  a.btn.btn-info(href="/courses/new") NEW
  
